const AWS = require('aws-sdk');
const { Pool } = require('pg');

// Database connection
const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  ssl: process.env.NODE_ENV === 'production' ? { rejectUnauthorized: false } : false,
});

// Update processing step status in database
async function updateProcessingStep(executionArn, stepName, status, stepOrder, inputData = null, outputData = null, errorDetails = null) {
  const query = `
    INSERT INTO processing_steps (execution_arn, step_name, step_order, status, input_data, output_data, error_details, started_at, completed_at)
    VALUES ($1, $2, $3, $4, $5, $6, $7, NOW(), $8)
    ON CONFLICT (execution_arn, step_order)
    DO UPDATE SET
      status = $4,
      output_data = $6,
      error_details = $7,
      completed_at = $8
  `;

  const completedAt = status === 'SUCCEEDED' || status === 'FAILED' ? new Date() : null;

  await pool.query(query, [
    executionArn,
    stepName,
    stepOrder,
    status,
    inputData,
    outputData,
    errorDetails,
    completedAt
  ]);
}

// Chunking strategies adapted from the existing DocumentChunker
const CHUNKING_STRATEGIES = {
  'single_chunk': {
    name: 'Single Chunk',
    maxTokens: 1200,
    minTokens: 0,
    overlapTokens: 0,
    preserveSections: false,
    useSemanticBoundaries: false,
  },
  'semantic': {
    name: 'Semantic Chunking',
    maxTokens: 1000,
    minTokens: 300,
    overlapTokens: 150,
    preserveSections: true,
    useSemanticBoundaries: true,
  },
  'technical_sections': {
    name: 'Technical Section-Based',
    maxTokens: 1200,
    minTokens: 200,
    overlapTokens: 100,
    preserveSections: true,
    useSemanticBoundaries: true,
  },
  'sds_sections': {
    name: 'SDS Section-Based',
    maxTokens: 800,
    minTokens: 100,
    overlapTokens: 50,
    preserveSections: true,
    useSemanticBoundaries: false,
  },
  'certificate': {
    name: 'Certificate Chunking',
    maxTokens: 600,
    minTokens: 200,
    overlapTokens: 100,
    preserveSections: false,
    useSemanticBoundaries: true,
  },
  'large_document': {
    name: 'Large Document Chunking',
    maxTokens: 900,
    minTokens: 400,
    overlapTokens: 200,
    preserveSections: true,
    useSemanticBoundaries: true,
  }
};\n\n// Estimate token count for text\nfunction estimateTokens(text) {\n  const words = text.split(/\\s+/).length;\n  const technicalTerms = (text.match(/[A-Z]{2,}|[\\w-]+-[\\w-]+|\\d+\\.\\d+/g) || []).length;\n  const chemicalFormulas = (text.match(/[A-Z][a-z]?\\d*(?:[A-Z][a-z]?\\d*)*/g) || []).length;\n\n  // Base estimation + technical complexity factor\n  return Math.ceil(words * 1.3 + technicalTerms * 0.2 + chemicalFormulas * 0.1);\n}\n\n// Generate unique chunk ID\nfunction generateChunkId() {\n  return `chunk_${Date.now()}_${Math.random().toString(36).substr(2, 9)}`;\n}\n\n// Calculate chunk quality score\nfunction calculateChunkQuality(text, sectionTitle = null) {\n  let quality = 70; // Base quality score\n\n  // Length appropriateness (50-2000 chars is optimal)\n  const length = text.length;\n  if (length >= 100 && length <= 2000) {\n    quality += 15;\n  } else if (length < 50) {\n    quality -= 20;\n  } else if (length > 3000) {\n    quality -= 10;\n  }\n\n  // Content coherence indicators\n  if ((text.match(/\\.\\s+[A-Z]/g) || []).length > 1) quality += 5; // Multiple sentences\n  if (text.includes('\\n')) quality += 5; // Structured content\n  if (sectionTitle) quality += 10; // Has section context\n\n  // Technical content indicators\n  if (text.match(/\\d+\\.\\d+/g)) quality += 5; // Numbers/measurements\n  if (text.match(/[A-Z]{3,}/g)) quality += 5; // Acronyms/technical terms\n\n  return Math.min(100, Math.max(0, quality));\n}\n\n// Select optimal chunking strategy\nfunction selectStrategy(documentType, tokenCount, metadata = {}) {\n  // Single chunk for small documents\n  if (tokenCount < 1200) {\n    return CHUNKING_STRATEGIES.single_chunk;\n  }\n\n  // Document type-specific strategies\n  const lowerType = documentType.toLowerCase();\n  \n  if (lowerType.includes('sds') || lowerType.includes('msds')) {\n    return CHUNKING_STRATEGIES.sds_sections;\n  }\n  \n  if (lowerType.includes('specification') || \n      lowerType.includes('technical') || \n      lowerType.includes('certificate_of_analysis')) {\n    return CHUNKING_STRATEGIES.technical_sections;\n  }\n  \n  if (lowerType.includes('certificate') || \n      lowerType.includes('halal') || \n      lowerType.includes('kosher') || \n      lowerType.includes('iso') || \n      lowerType.includes('reach')) {\n    return CHUNKING_STRATEGIES.certificate;\n  }\n\n  // Use large document strategy for very long texts\n  if (tokenCount > 5000) {\n    return CHUNKING_STRATEGIES.large_document;\n  }\n\n  return CHUNKING_STRATEGIES.semantic;\n}\n\n// Split text into sentences with better handling of abbreviations\nfunction splitIntoSentences(text) {\n  return text\n    .split(/(?<!\\b(?:Dr|Mr|Mrs|Ms|Prof|etc|vs|Inc|Ltd|Corp|Co)\\.)(?<![A-Z]\\.)\\. +(?=[A-Z])/g)\n    .flatMap(sentence => sentence.split(/[!?]+\\s+/))\n    .map(s => s.trim())\n    .filter(s => s.length > 0);\n}\n\n// Create a single chunk\nfunction createChunk(documentId, text, index, total, type, sectionTitle = null, sectionNumber = null) {\n  const chunkId = generateChunkId();\n  const tokenCount = estimateTokens(text);\n  const qualityScore = calculateChunkQuality(text, sectionTitle);\n\n  const metadata = {\n    chunk_id: chunkId,\n    document_id: documentId,\n    chunk_index: index,\n    chunk_total: total,\n    chunk_type: type,\n    section_title: sectionTitle,\n    section_number: sectionNumber,\n    has_overlap: false,\n    token_count: tokenCount,\n    character_count: text.length,\n    quality_score: qualityScore,\n  };\n\n  return {\n    chunk_id: chunkId,\n    document_id: documentId,\n    chunk_index: index,\n    chunk_total: total,\n    chunk_text: text.trim(),\n    chunk_type: type,\n    section_title: sectionTitle,\n    section_number: sectionNumber,\n    has_overlap: false,\n    metadata,\n  };\n}\n\n// Create single chunk for small documents\nfunction createSingleChunk(documentId, text, strategy) {\n  const chunk = createChunk(documentId, text, 0, 1, 'full_document');\n  chunk.metadata.quality_score = 100; // Full document always has perfect coverage\n  return [chunk];\n}\n\n// Extract SDS sections (1-16)\nfunction extractSDSSections(text) {\n  const sectionPattern = /(?:Section\\s+)?(\\d+)[.:\\s]+([^\\n]+)\\n([\\s\\S]*?)(?=(?:Section\\s+)?\\d+[.:\\s]+|$)/gi;\n  const sections = [];\n\n  let match;\n  while ((match = sectionPattern.exec(text)) !== null) {\n    const sectionNumber = parseInt(match[1]);\n\n    // Only process sections 1-16 for SDS\n    if (sectionNumber >= 1 && sectionNumber <= 16) {\n      sections.push({\n        number: sectionNumber,\n        title: match[2].trim(),\n        content: match[3].trim(),\n      });\n    }\n  }\n\n  return sections.sort((a, b) => a.number - b.number);\n}\n\n// Extract generic document sections\nfunction extractGenericSections(text) {\n  const headingPatterns = [\n    /^#{1,3}\\s+(.+)$/gm, // Markdown headings\n    /^([A-Z][A-Z\\s]{2,50}):?\\s*$/gm, // ALL CAPS headings\n    /^\\d+\\.\\s+([^.]+)$/gm, // Numbered headings\n    /^([A-Z][a-z]+(?:\\s+[A-Z][a-z]+)*):?\\s*$/gm, // Title Case headings\n  ];\n\n  const sections = [];\n  let lastIndex = 0;\n  let lastTitle = '';\n\n  for (const pattern of headingPatterns) {\n    let match;\n    pattern.lastIndex = 0; // Reset regex state\n    while ((match = pattern.exec(text)) !== null) {\n      if (lastTitle && match.index > lastIndex) {\n        const content = text.substring(lastIndex, match.index).trim();\n        if (content.length > 100) {\n          sections.push({ title: lastTitle, content });\n        }\n      }\n      lastTitle = match[1].trim();\n      lastIndex = match.index + match[0].length;\n    }\n  }\n\n  // Add final section\n  if (lastTitle && lastIndex < text.length) {\n    const content = text.substring(lastIndex).trim();\n    if (content.length > 100) {\n      sections.push({ title: lastTitle, content });\n    }\n  }\n\n  return sections;\n}\n\n// Get overlap sentences for chunk boundaries\nfunction getOverlapSentences(sentences, targetTokens) {\n  const overlap = [];\n  let tokens = 0;\n\n  for (let i = sentences.length - 1; i >= 0; i--) {\n    const sentenceTokens = estimateTokens(sentences[i]);\n    if (tokens + sentenceTokens > targetTokens) break;\n\n    overlap.unshift(sentences[i]);\n    tokens += sentenceTokens;\n  }\n\n  return overlap;\n}\n\n// Semantic boundary-based chunking\nfunction chunkSemanticBoundaries(documentId, text, strategy) {\n  const sentences = splitIntoSentences(text);\n  const chunks = [];\n\n  let currentChunk = [];\n  let currentTokens = 0;\n\n  for (let i = 0; i < sentences.length; i++) {\n    const sentence = sentences[i];\n    const sentenceTokens = estimateTokens(sentence);\n\n    // Check if adding this sentence would exceed max tokens\n    if (currentTokens + sentenceTokens > strategy.maxTokens && currentChunk.length > 0) {\n      // Finalize current chunk\n      const chunkText = currentChunk.join(' ').trim();\n      const chunk = createChunk(\n        documentId,\n        chunkText,\n        chunks.length,\n        0, // Will be updated later\n        'paragraph'\n      );\n      chunks.push(chunk);\n\n      // Start new chunk with overlap\n      if (strategy.overlapTokens > 0) {\n        const overlapSentences = getOverlapSentences(\n          currentChunk,\n          strategy.overlapTokens\n        );\n        currentChunk = overlapSentences;\n        currentTokens = estimateTokens(currentChunk.join(' '));\n      } else {\n        currentChunk = [];\n        currentTokens = 0;\n      }\n    }\n\n    currentChunk.push(sentence);\n    currentTokens += sentenceTokens;\n  }\n\n  // Add final chunk\n  if (currentChunk.length > 0) {\n    const chunkText = currentChunk.join(' ').trim();\n    const chunk = createChunk(\n      documentId,\n      chunkText,\n      chunks.length,\n      0,\n      'paragraph'\n    );\n    chunks.push(chunk);\n  }\n\n  // Update chunk_total and overlap flags for all chunks\n  chunks.forEach((chunk, index) => {\n    chunk.chunk_total = chunks.length;\n    chunk.metadata.chunk_total = chunks.length;\n    chunk.chunk_index = index;\n    chunk.metadata.chunk_index = index;\n    \n    // Update overlap flags\n    if (index > 0) {\n      chunk.has_overlap = true;\n      chunk.metadata.has_overlap = true;\n      chunk.metadata.overlap_with_previous = true;\n    }\n    if (index < chunks.length - 1) {\n      chunk.metadata.overlap_with_next = true;\n    }\n  });\n\n  return chunks;\n}\n\n// Chunk SDS documents by sections\nfunction chunkSDSDocument(documentId, text, strategy) {\n  const sections = extractSDSSections(text);\n  \n  if (sections.length === 0) {\n    // Fallback to semantic chunking if no sections found\n    return chunkSemanticBoundaries(documentId, text, strategy);\n  }\n\n  const chunks = [];\n\n  sections.forEach((section) => {\n    const tokenCount = estimateTokens(section.content);\n\n    if (tokenCount > strategy.maxTokens) {\n      // Split large sections\n      const sentences = splitIntoSentences(section.content);\n      let currentChunk = [];\n      let currentTokens = 0;\n      let subIndex = 0;\n\n      for (const sentence of sentences) {\n        const sentenceTokens = estimateTokens(sentence);\n\n        if (currentTokens + sentenceTokens > strategy.maxTokens && currentChunk.length > 0) {\n          const chunkText = currentChunk.join(' ');\n          const chunk = createChunk(\n            documentId,\n            chunkText,\n            chunks.length,\n            0,\n            'section',\n            `${section.title} (Part ${subIndex + 1})`,\n            section.number\n          );\n          chunks.push(chunk);\n          \n          currentChunk = [];\n          currentTokens = 0;\n          subIndex++;\n        }\n\n        currentChunk.push(sentence);\n        currentTokens += sentenceTokens;\n      }\n\n      // Add final sub-chunk\n      if (currentChunk.length > 0) {\n        const chunkText = currentChunk.join(' ');\n        const chunk = createChunk(\n          documentId,\n          chunkText,\n          chunks.length,\n          0,\n          'section',\n          `${section.title} (Part ${subIndex + 1})`,\n          section.number\n        );\n        chunks.push(chunk);\n      }\n    } else {\n      const chunk = createChunk(\n        documentId,\n        section.content,\n        chunks.length,\n        0,\n        'section',\n        section.title,\n        section.number\n      );\n      chunks.push(chunk);\n    }\n  });\n\n  // Update chunk_total for all chunks\n  chunks.forEach((chunk, index) => {\n    chunk.chunk_total = chunks.length;\n    chunk.metadata.chunk_total = chunks.length;\n    chunk.chunk_index = index;\n    chunk.metadata.chunk_index = index;\n  });\n\n  return chunks;\n}\n\n// Calculate quality metrics\nfunction calculateQualityMetrics(chunks, originalText) {\n  const totalOriginalLength = originalText.length;\n  const totalChunkLength = chunks.reduce((sum, chunk) => sum + chunk.chunk_text.length, 0);\n\n  // Coverage: how much of original text is preserved\n  const coverage_score = Math.min(100, (totalChunkLength / totalOriginalLength) * 100);\n\n  // Coherence: average chunk quality\n  const coherence_score = chunks.length > 0 ?\n    chunks.reduce((sum, chunk) => sum + chunk.metadata.quality_score, 0) / chunks.length : 0;\n\n  // Overlap efficiency: reasonable overlap without excessive duplication\n  const overlapChunks = chunks.filter(chunk => chunk.has_overlap).length;\n  const overlap_efficiency = chunks.length > 1 ?\n    Math.max(0, 100 - (overlapChunks / chunks.length) * 50) : 100;\n\n  return {\n    coherence_score: Math.round(coherence_score),\n    coverage_score: Math.round(coverage_score),\n    overlap_efficiency: Math.round(overlap_efficiency),\n  };\n}\n\n// Main chunking function\nfunction chunkDocument(documentId, text, documentType, tokenCount, metadata = {}) {\n  const strategy = selectStrategy(documentType, tokenCount, metadata);\n  let chunks;\n\n  console.log(`Using chunking strategy: ${strategy.name} for document type: ${documentType}`);\n\n  // Apply strategy-specific chunking\n  switch (strategy.name) {\n    case 'Single Chunk':\n      chunks = createSingleChunk(documentId, text, strategy);\n      break;\n    case 'SDS Section-Based':\n      chunks = chunkSDSDocument(documentId, text, strategy);\n      break;\n    case 'Technical Section-Based':\n      const technicalSections = extractGenericSections(text);\n      if (technicalSections.length > 1) {\n        chunks = chunkBySections(documentId, technicalSections, strategy);\n      } else {\n        chunks = chunkSemanticBoundaries(documentId, text, strategy);\n      }\n      break;\n    case 'Certificate Chunking':\n      const paragraphs = text.split(/\\n\\s*\\n/).filter(p => p.trim().length > 50);\n      if (paragraphs.length <= 2) {\n        chunks = createSingleChunk(documentId, text, strategy);\n      } else {\n        chunks = chunkByParagraphs(documentId, paragraphs, strategy);\n      }\n      break;\n    default:\n      chunks = chunkSemanticBoundaries(documentId, text, strategy);\n  }\n\n  // Calculate quality metrics\n  const qualityMetrics = calculateQualityMetrics(chunks, text);\n\n  return {\n    chunks,\n    strategy_used: strategy.name,\n    total_chunks: chunks.length,\n    total_tokens: chunks.reduce((sum, chunk) => sum + chunk.metadata.token_count, 0),\n    average_chunk_size: chunks.length > 0 ?\n      chunks.reduce((sum, chunk) => sum + chunk.metadata.token_count, 0) / chunks.length : 0,\n    quality_metrics: qualityMetrics,\n  };\n}\n\n// Helper function for section-based chunking\nfunction chunkBySections(documentId, sections, strategy) {\n  const chunks = [];\n\n  sections.forEach((section) => {\n    const tokenCount = estimateTokens(section.content);\n\n    if (tokenCount > strategy.maxTokens) {\n      // Split large sections\n      const sentences = splitIntoSentences(section.content);\n      let currentChunk = [];\n      let currentTokens = 0;\n      let subIndex = 0;\n\n      for (const sentence of sentences) {\n        const sentenceTokens = estimateTokens(sentence);\n\n        if (currentTokens + sentenceTokens > strategy.maxTokens && currentChunk.length > 0) {\n          const chunkText = currentChunk.join(' ');\n          const chunk = createChunk(\n            documentId,\n            chunkText,\n            chunks.length,\n            0,\n            'section',\n            `${section.title} (Part ${subIndex + 1})`\n          );\n          chunks.push(chunk);\n          \n          currentChunk = [];\n          currentTokens = 0;\n          subIndex++;\n        }\n\n        currentChunk.push(sentence);\n        currentTokens += sentenceTokens;\n      }\n\n      if (currentChunk.length > 0) {\n        const chunkText = currentChunk.join(' ');\n        const chunk = createChunk(\n          documentId,\n          chunkText,\n          chunks.length,\n          0,\n          'section',\n          `${section.title} (Part ${subIndex + 1})`\n        );\n        chunks.push(chunk);\n      }\n    } else {\n      const chunk = createChunk(\n        documentId,\n        section.content,\n        chunks.length,\n        0,\n        'section',\n        section.title\n      );\n      chunks.push(chunk);\n    }\n  });\n\n  // Update totals\n  chunks.forEach((chunk, index) => {\n    chunk.chunk_total = chunks.length;\n    chunk.metadata.chunk_total = chunks.length;\n    chunk.chunk_index = index;\n    chunk.metadata.chunk_index = index;\n  });\n\n  return chunks;\n}\n\n// Helper function for paragraph-based chunking\nfunction chunkByParagraphs(documentId, paragraphs, strategy) {\n  const chunks = [];\n  let currentChunk = [];\n  let currentTokens = 0;\n\n  for (const paragraph of paragraphs) {\n    const paragraphTokens = estimateTokens(paragraph);\n\n    if (currentTokens + paragraphTokens > strategy.maxTokens && currentChunk.length > 0) {\n      const chunkText = currentChunk.join('\\n\\n');\n      const chunk = createChunk(documentId, chunkText, chunks.length, 0, 'paragraph');\n      chunks.push(chunk);\n\n      currentChunk = [];\n      currentTokens = 0;\n    }\n\n    currentChunk.push(paragraph);\n    currentTokens += paragraphTokens;\n  }\n\n  if (currentChunk.length > 0) {\n    const chunkText = currentChunk.join('\\n\\n');\n    const chunk = createChunk(documentId, chunkText, chunks.length, 0, 'paragraph');\n    chunks.push(chunk);\n  }\n\n  // Update totals\n  chunks.forEach((chunk, index) => {\n    chunk.chunk_total = chunks.length;\n    chunk.metadata.chunk_total = chunks.length;\n    chunk.chunk_index = index;\n    chunk.metadata.chunk_index = index;\n  });\n\n  return chunks;\n}\n\n// Main Lambda handler\nexports.handler = async (event) => {\n  const startTime = Date.now();\n  console.log('Document chunking event:', JSON.stringify(event, null, 2));\n\n  const { executionId, documentId, extractedText, metadata } = event;\n  const executionArn = `arn:aws:states:${process.env.AWS_REGION}:${process.env.AWS_ACCOUNT_ID}:execution:DocumentProcessingWorkflow:${executionId}`;\n\n  try {\n    // Update step status to RUNNING\n    await updateProcessingStep(executionArn, 'DocumentChunking', 'RUNNING', 4, event);\n\n    // Validate input\n    if (!extractedText || extractedText.trim().length === 0) {\n      throw new Error('No text provided for chunking');\n    }\n\n    console.log(`Starting chunking for document ${documentId}, text length: ${extractedText.length}`);\n\n    // Estimate total tokens for strategy selection\n    const totalTokens = estimateTokens(extractedText);\n    \n    // Determine document type from metadata or filename\n    const documentType = metadata?.documentType || \n                        (metadata?.filename && detectDocumentTypeFromFilename(metadata.filename)) || \n                        'general';\n\n    // Perform chunking\n    const chunkingResult = chunkDocument(\n      documentId,\n      extractedText,\n      documentType,\n      totalTokens,\n      metadata\n    );\n\n    console.log(`Chunking completed: ${chunkingResult.total_chunks} chunks created using ${chunkingResult.strategy_used}`);\n\n    // Prepare the result\n    const result = {\n      chunks: chunkingResult.chunks,\n      totalChunks: chunkingResult.total_chunks,\n      strategyUsed: chunkingResult.strategy_used,\n      totalTokens: chunkingResult.total_tokens,\n      averageChunkSize: Math.round(chunkingResult.average_chunk_size),\n      qualityMetrics: chunkingResult.quality_metrics,\n      processingTimeMs: Date.now() - startTime,\n      timestamp: new Date().toISOString()\n    };\n\n    // Update step status to SUCCEEDED\n    await updateProcessingStep(executionArn, 'DocumentChunking', 'SUCCEEDED', 4, event, result);\n\n    return result;\n\n  } catch (error) {\n    console.error('Document chunking error:', error);\n\n    const errorDetails = {\n      message: error.message,\n      stack: error.stack,\n      processingTimeMs: Date.now() - startTime\n    };\n\n    // Update step status to FAILED\n    try {\n      await updateProcessingStep(executionArn, 'DocumentChunking', 'FAILED', 4, event, null, errorDetails);\n    } catch (dbError) {\n      console.error('Failed to update step status:', dbError);\n    }\n\n    throw error;\n  }\n};\n\n// Helper function to detect document type from filename\nfunction detectDocumentTypeFromFilename(filename) {\n  const lower = filename.toLowerCase();\n  \n  if (lower.includes('sds') || lower.includes('msds') || lower.includes('safety')) {\n    return 'sds';\n  }\n  if (lower.includes('specification') || lower.includes('spec')) {\n    return 'specification';\n  }\n  if (lower.includes('certificate') || lower.includes('cert')) {\n    return 'certificate';\n  }\n  if (lower.includes('technical') || lower.includes('datasheet')) {\n    return 'technical_data_sheet';\n  }\n  if (lower.includes('analysis') || lower.includes('coa')) {\n    return 'certificate_of_analysis';\n  }\n  \n  return 'general';\n}"